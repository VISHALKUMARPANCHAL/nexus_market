<?php
$cartItems = $this->getItems();
$isEmpty = empty($cartItems);
?>

<section class="py-5 bg-light">
    <div class="container py-4">
        <div class="row">
            <div class="col-lg-10 mx-auto">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="fw-bold text-primary mb-0">Your Shopping Cart</h2>
                    <?php if (!$isEmpty): ?>
                    <span class="badge bg-primary rounded-pill"><?php echo count($cartItems); ?> items</span>
                    <?php endif; ?>
                </div>

                <?php if ($isEmpty): ?>
                <div class="card shadow-sm border-0 rounded-3 mb-4">
                    <div class="card-body p-5 text-center">
                        <div class="mb-4">
                            <i class="fas fa-shopping-cart text-muted" style="font-size: 4rem;"></i>
                        </div>
                        <h3 class="fw-bold mb-3">Your cart is empty</h3>
                        <p class="text-muted mb-4">Looks like you haven't added any products to your cart yet.</p>
                        <a href="<?php echo $this->getUrl('catalog/product/list') ?>"
                            class="btn btn-primary btn-lg px-5">
                            <i class="fas fa-store me-2"></i> Browse Products
                        </a>
                    </div>
                </div>
                <?php else: ?>
                <div class="card shadow-sm border-0 rounded-3 mb-4">
                    <div class="card-header bg-white py-3">
                        <h5 class="fw-bold mb-0">Cart Items</h5>
                    </div>
                    <div class="card-body p-0">
                        <?php foreach ($cartItems as $cartItem): ?>
                        <div class="row g-0 p-4 border-bottom">
                            <div class="col-md-2 col-lg-2">
                                <img src="<?php echo Mage::getBaseUrl() . $cartItem->getProduct()->getFilePath()[array_search(1, $cartItem->getProduct()->getMainImage())] ?>"
                                    class="img-fluid rounded-3 shadow-sm"
                                    alt="<?php echo $cartItem->getProduct()->getName() ?>">
                            </div>

                            <div class="col-md-5 col-lg-5 ps-md-4">
                                <h5 class="fw-bold text-dark mb-1"><?php echo $cartItem->getProduct()->getName() ?></h5>
                                <p class="text-muted small mb-0"><?php echo $cartItem->getProduct()->getDescription() ?>
                                </p>
                                <div class="d-flex align-items-center mt-3">
                                    <span class="me-2">Unit Price:</span>
                                    <h6 class="fw-bold mb-0">₹<?php echo $cartItem->getPrice() ?></h6>
                                </div>
                            </div>

                            <form action="<?php echo $this->getUrl('*/*/update') ?>" method="post"
                                class="col-md-2 col-lg-2 d-flex align-items-center justify-content-center">
                                <div class="text-center">
                                    <div class="input-group input-group-sm border rounded">
                                        <button class="btn btn-outline-secondary border-0" type="button"
                                            onclick="this.parentNode.querySelector('input[type=number]').stepDown(); this.form.submit();">
                                            <i class="fas fa-minus"></i>
                                        </button>
                                        <input type="hidden" name="item_id"
                                            value="<?php echo $cartItem->getItemId() ?>">
                                        <input id="form1" min="1" name="quantity"
                                            value="<?php echo $cartItem->getProductQuantity() ?>" type="number"
                                            class="form-control text-center border-0 bg-transparent" />

                                        <button class="btn btn-outline-secondary border-0" type="button"
                                            onclick="this.parentNode.querySelector('input[type=number]').stepUp(); this.form.submit();">
                                            <i class="fas fa-plus"></i>
                                        </button>
                                    </div>
                                </div>
                            </form>

                            <div class="col-md-2 col-lg-2 text-end d-flex align-items-center justify-content-end">
                                <div>
                                    <h5 class="fw-bold mb-0">₹<?php echo $cartItem->getSubTotal() ?></h5>
                                    <span class="text-muted small">subtotal</span>
                                </div>
                            </div>

                            <div class="col-md-1 col-lg-1 text-end d-flex align-items-center justify-content-end">
                                <a href="<?php echo $this->getUrl('*/*/remove') ?>/?id=<?php echo $cartItem->getItemId() ?>"
                                    class="btn btn-sm btn-link text-danger p-0" title="Remove item">
                                    <i class="fas fa-trash"></i>
                                </a>
                            </div>
                        </div>
                        <?php endforeach; ?>
                    </div>
                </div>

                <div class="card shadow-sm border-0 rounded-3 mb-4">
                    <div class="card-header bg-white py-3">
                        <h5 class="fw-bold mb-0">Discount Code</h5>
                    </div>
                    <div class="card-body">
                        <form action="<?php echo $this->getUrl('*/*/applyCoupon') ?>" method="post">
                            <div class="input-group">
                                <input type="text" class="form-control m-0" name="coupon_code"
                                    value="<?php echo $this->getCart()->getCouponCode() ?>"
                                    placeholder="Enter coupon code" aria-label="Coupon Code"
                                    <?php echo ($this->getCart()->getCouponCode()) ? "readonly" : "" ?>>
                                <input type="hidden" value="<?php echo $this->getTotalAmount() ?>" name="sub_total">
                                <button
                                    class="btn <?php echo ($this->getCart()->getCouponCode()) ? "btn-danger" : "btn-primary" ?> m-0"
                                    type="submit" name="type"
                                    value="<?php echo ($this->getCart()->getCouponCode()) ? "Remove" : "Apply" ?>"><?php echo ($this->getCart()->getCouponCode()) ? "Remove" : "Apply" ?></button>
                            </div>
                        </form>
                    </div>
                </div>


                <div class="card shadow-sm border-0 rounded-3">
                    <div class="card-header bg-white py-3">
                        <h5 class="fw-bold mb-0">Order Summary</h5>
                    </div>
                    <div class="card-body">
                        <div class="row mb-4">
                            <div class="col-lg-8">
                                <div class="d-flex justify-content-between mb-2">
                                    <span>Subtotal</span>
                                    <span>₹<?php
                                                $subtotal = $this->getTotalAmount();
                                                echo $subtotal;
                                                ?></span>
                                </div>
                                <?php if ($this->getCart()->getShippingMethod()): ?>
                                <div class="d-flex justify-content-between mb-2">
                                    <span>Shipping</span>
                                    <span class="text-success">
                                        <?php
                                                echo ($this->getCart()->getShippingPrice() == 0) ? "Free" :
                                                    '+₹' . $this->getCart()->getShippingPrice(); ?>
                                    </span>
                                </div>
                                <?php endif; ?>
                                <?php if ($this->getCart()->getCouponCode()): ?>
                                <div class="d-flex justify-content-between mb-2">
                                    <span>Discount</span>
                                    <span
                                        class="text-success">-₹<?php echo $this->getCart()->getCouponDiscount() ?></span>
                                </div>
                                <?php endif; ?>

                                <hr>
                                <div class="d-flex justify-content-between mb-2">
                                    <span class="fw-bold">Total</span>
                                    <span class="fw-bold">₹<?php echo $this->getCart()->getTotalAmount(); ?></span>
                                </div>
                            </div>
                            <div class="col-lg-4 d-flex align-items-end">
                                <a type="button" href="<?php echo $this->getUrl('*/cart_address/index'); ?>"
                                    class="btn btn-primary btn-lg w-100 text-uppercase py-3"
                                    onclick="document.getElementById('shipping-billing-form').submit();">
                                    <i class="fas fa-lock me-2"></i> Checkout
                                </a>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <div class="d-flex justify-content-between align-items-center">
                                    <a href="<?php echo $this->getUrl('catalog/product/list') ?>"
                                        class="text-decoration-none">
                                        <i class="fas fa-long-arrow-alt-left me-2"></i> Continue Shopping
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</section>