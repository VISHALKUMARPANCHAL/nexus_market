<?php
$address = $this->getAddress();
$id = $this->getAddressId();
?>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0"><?php echo ($id) ? "Update" : "Add" ?> Address</h3>
                </div>

                <div class="card-body">
                    <form id="registrationForm" method="post" action="<?php echo $this->getUrl('*/*/save'); ?>"
                        class="needs-validation">
                        <input type="hidden" value="<?php echo ($id) ? $address->getAddressId() : "" ?>"
                            name="customer_account_address[address_id]">
                        <!-- Street Address -->
                        <div class="mb-3">
                            <label for="street_address" class="form-label">Street Address</label>
                            <input type="text" class="form-control" id="street_address"
                                value="<?php echo ($id) ? $address->getStreetAddress() : "" ?>"
                                name="customer_account_address[street_address]" required>

                        </div>

                        <div class="row">
                            <!-- City -->
                            <div class="col-md-6 mb-3">
                                <label for="city" class="form-label">City</label>
                                <input type="text" class="form-control" id="city"
                                    value="<?php echo ($id) ? $address->getCity() : "" ?>"
                                    name="customer_account_address[city]" required>

                            </div>

                            <!-- State -->
                            <div class="col-md-6 mb-3">
                                <label for="state" class="form-label">Region</label>
                                <input type="text" class="form-control" id="region"
                                    value="<?php echo ($id) ? $address->getRegion() : "" ?>"
                                    name="customer_account_address[region]" required>

                            </div>
                        </div>

                        <div class="row">
                            <!-- Zipcode -->
                            <div class="col-md-4 mb-3">
                                <label for="zipcode" class="form-label">Zipcode</label>
                                <input type="text" class="form-control" id="zipcode"
                                    value="<?php echo ($id) ? $address->getZipcode() : "" ?>"
                                    name="customer_account_address[zipcode]" required>

                            </div>
                            <!-- Country -->
                            <div class="col-md-4 mb-3">
                                <label for="country" class="form-label">Country</label>
                                <select class="form-select" id="country" name="customer_account_address[country]">
                                    <option value="">Select Country</option>
                                    <option value="India"
                                        <?php echo ($id && $address->getCountry() == "India") ? "selected" : "" ?>>
                                        India</option>
                                    <option value="United States"
                                        <?php echo ($id && $address->getCountry() == "United States") ? "selected" : "" ?>>
                                        United States</option>
                                    <option value="Canada"
                                        <?php echo ($id && $address->getCountry() == "Canada") ? "selected" : "" ?>>
                                        Canada</option>
                                    <option value="United Kingdom"
                                        <?php echo ($id && $address->getCountry() == "United Kingdom") ? "selected" : "" ?>>
                                        United Kingdom</option>
                                </select>

                            </div>

                        </div>



                        <!-- Submit Button -->
                        <div class="d-grid mt-4">
                            <button type="submit"
                                class="btn btn-primary"><?php echo ($id) ? "Update" : "Add" ?></button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>