<?php
$categories = $this->getData();
?>

<br>

<div class="container mw-100">
    <div class="row">
        <div class="col-12">
            <div class="card shadow rounded-3 mb-5">
                <div class="card-header bg-primary text-white">
                    <h2 class="m-0 text-white">Category Admin</h2>
                </div>
                <div class="card-body">
                    <a href="./new" class="btn btn-success mb-4">
                        <i class="fas fa-plus-circle me-2"></i>Insert New Category
                    </a>
                    <?php $this->getChildHtml('csv') ?>


                    <div class="table-responsive">
                        <table class="table table-striped table-hover align-middle mb-0" id="producttable1">
                            <thead class="table-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Parent Category</th>
                                    <th>Update</th>
                                    <th>Delete</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($categories as $category): ?>
                                <tr>
                                    <td><span
                                            class="badge bg-secondary"><?php echo htmlspecialchars($category->getCategoryId()); ?></span>
                                    </td>
                                    <td class="fw-bold"><?php echo htmlspecialchars($category->getName()); ?>
                                    </td>
                                    <td><?php echo htmlspecialchars($category->getDescription()); ?></td>
                                    <td>
                                        <?php if ($category->getParentId()): ?>
                                        <span
                                            class="badge bg-info text-dark"><?php echo htmlspecialchars($this->getParentName(($category->getParentId()))); ?></span>
                                        <?php else: ?>
                                        <span class="badge bg-dark">Top Level</span>
                                        <?php endif; ?>
                                    </td>
                                    <td>
                                        <a href="./new/?id=<?php echo $category->getCategoryId(); ?>"
                                            class="text-primary mx-1 fs-5" title="Edit">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    </td>
                                    <td>
                                        <a href="./delete/?id=<?php echo $category->getCategoryId(); ?>"
                                            onclick="return confirm('Are you sure you want to delete this category?')"
                                            class="text-danger mx-1 fs-5" title="Delete">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                    <?php $this->getChildHtml('toolbar') ?>
                </div>
            </div>
        </div>
    </div>
</div>