<?php
$products = $this->getData();
?>
<br>
<div class="container mw-100">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-primary">
                    <h2 class="m-0 text-white">Product Admin</h2>
                </div>
                <div class="card-body">
                    <a href="./new" class="btn btn-success btn-insert">
                        <i class="fas fa-plus-circle me-2"></i>Insert New Product
                    </a>

                    <div class="table-responsive">
                        <table class="table table-striped table-hover align-middle" id="producttable1">
                            <thead class="table-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>SKU</th>
                                    <th>Price</th>
                                    <th>Stock</th>
                                    <th>Category</th>
                                    <th>Material</th>
                                    <th>Weight</th>
                                    <th>Series</th>
                                    <th>Update</th>
                                    <th>Delete</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($products as $product): ?>
                                <tr>
                                    <td>
                                        <span class="badge bg-secondary">
                                            <?php echo htmlspecialchars($product->getProductId()); ?>
                                    </td>
                                    </span>
                                    <td><?php echo htmlspecialchars($product->getName()); ?></td>
                                    <td><?php echo htmlspecialchars($product->getDescription()); ?></td>
                                    <td><?php echo htmlspecialchars($product->getSku()); ?></td>
                                    <td>₹<?php echo htmlspecialchars($product->getPrice()); ?></td>
                                    <td>
                                        <span
                                            class="badge <?php echo $product->getStockQuantity() > 10 ? 'bg-success' : 'bg-danger'; ?>">
                                            <?php echo htmlspecialchars($product->getStockQuantity()); ?>
                                        </span>
                                    </td>
                                    <td>
                                        <span class="badge bg-info text-dark">
                                            <?php echo htmlspecialchars($this->getCategoryName($product->getCategoryId())); ?>
                                        </span>
                                    </td>
                                    <td><?php echo htmlspecialchars($product->getMaterial()); ?></td>
                                    <td><?php echo htmlspecialchars($product->getWeight()); ?></td>
                                    <td><?php echo htmlspecialchars($product->getSeries()); ?></td>
                                    <td class="action-icons">
                                        <a href="./new/?id=<?php echo $product->getProductId(); ?>" class="text-primary"
                                            title="Edit">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    </td>
                                    <td class="action-icons">
                                        <a href="./delete/?id=<?php echo $product->getProductId(); ?>"
                                            onclick="return confirm('Are you sure you want to delete this product?')"
                                            class="text-danger" title="Delete">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>