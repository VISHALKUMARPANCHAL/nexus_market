<?php
$orderData = $this->getOrder();

?>

<div class="container mt-4 mb-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-shopping-cart"></i> Order Details</h2>
        <div>
            <button onclick="window.print()" class="btn btn-outline-secondary me-2">
                <i class="fas fa-print"></i> Print
            </button>
            <a href="<?php echo $this->getUrl('*/*/index') ?>" class="btn btn-outline-primary">
                <i class="fas fa-arrow-left"></i> Back to Orders
            </a>
        </div>
    </div>
    <!-- Order Summary Card -->
    <div class="card mb-4">
        <?php $this->getChildHtml('orderinfo') ?>
    </div>

    <!-- Addresses Section -->
    <div class="row">
        <?php $this->getChildHtml('addressinfo') ?>
    </div>

    <!-- Order Items -->
    <div class="card">
        <?php $this->getChildHtml('iteminfo') ?>
    </div>

    <!-- Order Totals -->
    <div class="row mt-4">
        <div class="col-md-6 ms-auto">
            <div class="card">
                <div class="card-header bg-light">
                    <h5 class="mb-0"><i class="fas fa-calculator"></i> Order Summary</h5>
                </div>
                <div class="card-body">
                    <div class="d-flex justify-content-between mb-2">
                        <span>Subtotal:</span>
                        <span>₹<?php echo $this->getSubTotal() ?></span>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                        <span>Coupon Discount (<?php echo $orderData->getCouponCode() ?>):</span>
                        <span class="text-danger">-₹<?php echo $orderData->getCouponDiscount() ?></span>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                        <span>Shipping (Express):</span>
                        <span>₹<?php echo $orderData->getShippingPrice() ?></span>
                    </div>
                    <hr>
                    <div class="d-flex justify-content-between fw-bold">
                        <span>Total:</span>
                        <span>₹<?php echo $orderData->getTotalAmount() ?></span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Action Buttons -->
    <!-- <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-light">
                    <h5 class="mb-0"><i class="fas fa-tasks"></i> Order Actions</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="orderStatus" class="form-label">Update Order Status</label>
                            <select class="form-select" id="orderStatus">
                                <option value="" selected>Pending</option>
                                <option value="processing">Processing</option>
                                <option value="shipped">Shipped</option>
                                <option value="delivered">Delivered</option>
                                <option value="cancelled">Cancelled</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="trackingNumber" class="form-label">Tracking Number</label>
                            <input type="text" class="form-control" id="trackingNumber"
                                placeholder="Enter tracking number">
                        </div>
                    </div>
                    <div class="d-flex justify-content-end mt-3">
                        <button class="btn btn-success me-2"><i class="fas fa-save"></i> Save Changes</button>
                        <button class="btn btn-danger"><i class="fas fa-trash"></i> Cancel Order</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> -->